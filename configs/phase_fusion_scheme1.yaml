seed: 42
fs: 16000
segment_length: 4.0

speech_dir: data/chunks/bigspeech/
noise_dir: data/chunks/bignoise/

addse_cfg: configs/addse-s-mydata-eval-3metrics.yaml
addse_ckpt: logs/addse-edbase-quick/checkpoints/addse-s.ckpt
phase_cnn_ckpt: experiments/phase_branch2/weights/phase_cnn_branch2.pt

adapter_ckpt: experiments/phase_fusion_scheme1/weights/adapter.pt
report_json: experiments/phase_fusion_scheme1/reports/report_100.json

train_steps: 30
train_batch_size: 1
eval_examples: 100
num_workers: 0

lr: 0.0001
lambda_phase: 0.3
fusion_scale: 0.2
phase_delta_clip: 1.2

snr_min: 0.0
snr_max: 10.0

frame_length: 512
hop_length: 256
n_fft: 512
